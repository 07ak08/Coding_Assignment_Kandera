# Coding_Assignment_Kandera

How to reproduce this work:

clone the respository and use the dataset airbnb_london_listing
reproducing the python code:

install the necessary packages in python (pandas, numpy, matplotlib, os)
clean the data
prepare the data for analysis
produce summary statistics
generate a histogram
reproducing the stata code:

the first do file demonstrates folder structure
the second do file cleans the data
the third do file prepares the data for analysis
the fourth do file does summary statistics
the last do file generates a graph
